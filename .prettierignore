node_modules/
dist/
build/
out/
coverage/
.expo/
pnpm-lock.yaml

# Planning docs — owned by the planning process, not by Prettier.
.chalk/

# Native projects generated by `expo prebuild`. Owned by the Expo CLI's
# templates, not by Prettier — formatting them produces churn the next
# prebuild would just overwrite.
apps/mobile/ios/
apps/mobile/android/

# Vendored third-party source (git submodules). Owned upstream; not
# our place to reformat. See vendor/README.md.
vendor/

# E2E run artifact — written by `scripts/e2e/run.ts` into the cwd,
# never checked in.
e2e-report.json
