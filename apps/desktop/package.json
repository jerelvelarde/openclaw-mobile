{
  "name": "@openclaw/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Stub. Real Electron scaffolding lands in plan P02B.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo '[desktop] lint stub — real config lands in P02B' && exit 0",
    "test": "echo '[desktop] no tests yet — landing in P02B' && exit 0"
  }
}
