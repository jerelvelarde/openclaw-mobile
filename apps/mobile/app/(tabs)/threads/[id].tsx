// Thread detail placeholder (Chat | Canvas | Tools). Real screen lands in
// P06A (chat) / P05A (Canvas inline) / P07A (tool inspector).
import { Text, View } from 'react-native';

export default function ThreadDetailScreen() {
  return (
    <View>
      <Text>Thread detail (P06A)</Text>
    </View>
  );
}
