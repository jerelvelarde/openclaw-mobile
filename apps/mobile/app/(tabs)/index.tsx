// Home tab placeholder. Real screen (active agent, recent threads, voice
// CTA) lands in P05A.
import { Text, View } from 'react-native';

export default function HomeScreen() {
  return (
    <View testID="home-screen">
      <Text>Home (P05A)</Text>
    </View>
  );
}
